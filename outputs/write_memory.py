# -*- coding: utf-8 -*-
"""追加今日工作记忆：通达信本地 vipdoc 板块多周期共振分析。"""
import os, datetime

D = r'D:\vcp_hunter\产业链投研\.workbuddy\memory'
LOG = os.path.join(D, '2026-09-21.md')

NOTE = """

## 通达信本地板块多周期共振分析（多周期共振专家，2026-09-21 晚）

同批 42+84=126 只标的第二次分析（白天那次是「临界度」框架），本轮用**多周期共振框架**。
产物：`deliverables/板块多周期共振分析_20260921.md`、`板块个股完整清单_20260921.csv`、`多周期共振_20260921.html`。

**关键方法论突破：本地 vipdoc 的除权甄别（可复用，重要）**

1. **本地 `.day` 是不复权数据**，直接用会失真。`D:\\HT` 没有可用的 gbbq——
   `T0002\\hq_cache\\gbbq` 存在且配套 `gbbq.map`（文本：6位代码→偏移），但**正文疑似加密，无法本地解析**，
   不要浪费时间尝试，直接走 MCP 标定路线。

2. **跳空性质甄别的硬判据 = 是否突破法定涨跌幅限制**（沪主板/深主板 ±10%、科创/创业 ±20%、北交所 ±30%，+0.5pp 容差）。
   超限 → 必为除权；未超限 → 真实成交。这一步极关键：
   初检 130 日窗口有 59 只单日跌超 11%，看似大面积除权污染，实际**只有 12 只是真除权**，
   其余是 2026 年 5-8 月的系统性暴跌（07-17 创业板 -7.15%/科创50 -7.12%、07-28 创业板 -7.35%、08-19 创业板 -6.26%）。
   07-20 更典型：指数微涨（创业板 +0.42%）而 AI 板块个股成片跌停，是真实板块二次崩盘，不是数据错误。
   **不要一看到同步跳空就判定数据源有问题**——先对指数。

3. **复权因子用 tdx MCP 前复权日线标定**（不是自己推算比例）：
   `startxh = M-1-i, wantNum=2` 恰好返回除权日及其前一日两根（M=本地**有效**序列长度，i=除权日正向 index）。
   因子 `f_prev = fq_close[prev] / raw_close[prev]`，应用于 index<=prev 的全部 OHLC；`f_cur` 同理。
   本次标定 11 只（源杰0.689 / 寒武纪0.671 / 天孚0.712 / 新易盛0.713 / 腾景0.714 / 强瑞0.711 / 炬光0.690 /
   瑞可达0.713 / 博杰0.769 / 英维克0.768 / 肯特0.767）。

4. **⚠ `.day` 有效记录数 ≠ len(file)//32**：文件里混有 d<=0 / c<=0 的废行，
   read_day 会过滤掉。算 startxh / index 必须用**过滤后的序列**，否则偏移整体差 1（本次踩到，MCP 返回日期对不上才发现）。

5. **检测窗口 130 个交易日足够，不必扫全序列**。MACD 的 EMA 冲击响应 (1-α)^N：
   EMA12 α=2/13，130 根后衰减至 ~1e-10。全序列扫会命中 84 只（含 1993/2005 年的历史除权），纯噪音。
   白天那份报告报的「16 只除权」应该是用了更宽窗口，与本轮 12 只的差异无实质影响。

**两板块格局**：关注个股 42 只中 13 只三周期共振向上（创新药/CXO 主线强势）；
AI硬件 84 只中仅 3 只（有研硅/源杰科技/华正新材），77 只周期错位——该板块 7 月遭极端杀跌后，
多数月线仍多头但周/日线未修复，属「大周期向上、中短期休整」。

**两板块明日关注名单（结构健康度口径，非涨跌预测）**
- 关注个股：603259 药明康德（今日日MACD刚金叉、BIAS20仅6.8%最低、VR5 1.40放量）、301047 义翘神州、
  688046 药康生物、688690 纳微科技、002821 凯莱英。
- AI硬件：688432 有研硅（唯一共振且RSI仅57未超买，但 ATR% 10.8 极高需压缩仓位）、300548 长芯博创、
  688313 仕佳光子、300570 太辰光、603228 景旺电子（日线MACD死叉，中期强短线待企稳）。
- 过热剔除：688222 成都先导（BIAS20 34%+RSI83+顶背离）、301080 百普赛斯、688293 奥浦迈、000739 普洛药业（贴顶）、
  688498 源杰科技（RSI 84.9）、603186 华正新材（跌破MA5+VR5 0.77缩量）。
"""


def main():
    old = ''
    if os.path.isfile(LOG):
        old = open(LOG, encoding='utf-8').read()
    if '多周期共振专家，2026-09-21 晚' in old:
        print('already logged, skip')
        return
    with open(LOG, 'a', encoding='utf-8') as f:
        f.write(NOTE)
    t = open(LOG, encoding='utf-8').read()
    print('append ok, total chars=%d, 乱码=%d' % (len(t), t.count('\ufffd')))


if __name__ == '__main__':
    main()
