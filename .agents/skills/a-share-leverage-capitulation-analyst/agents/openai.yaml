interface:
  display_name: "两融去杠杆压力分析"
  short_description: "用 DFCF 日更两融表并分析指数急跌与市场宽度共振"
  default_prompt: "Use $a-share-leverage-capitulation-analyst to update the DFCF-only margin table and analyze A-share deleveraging pressure."
policy:
  allow_implicit_invocation: false
