interface:
  display_name: "Kronos K 线预测"
  short_description: "运行本地 Kronos-base K 线预测并核验输入与输出"
  default_prompt: "Use $kronos-market-forecasting to validate this OHLCV input, run the local Kronos-base forecast, and report model_output with limitations."
policy:
  allow_implicit_invocation: true
