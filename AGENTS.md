# 产业链投研项目规则

## 响应语言与通用要求 (Response Language)

- 无论用户输入使用何种语言，一律使用中文回答（Always reply in Chinese, regardless of the language used in the user's input）。
- 仅在代码、命令、文件名、API、精确引用或用户明确要求的产出物中保留非中文文本。

## 技能优先与路由规则 (Skills First & Skill Routing)

- 每次回答或任务执行前，先检查可用的技能库并选择合适的技能或技能组合。
- 如果任务明确匹配某个技能，在回答或编辑文件前，必须阅读并遵循相关 `SKILL.md` 指引。
- 仓库技能位于 `.agents/skills/`，由 Codex/Antigravity 自动发现；项目领域路由仅查询本项目技能。账户级已安装数据 Skill 只作配套能力，不复制进仓库。
- 按问题语义选择唯一领域入口：上市公司深研用 `research-listed-company`，产业链用 `research-industry-chain`，财报/电话会用 `earnings-call-investment-analyst`，收益安全用 `income-investment`，持续跟踪用 `a-share-company-tracking`，两融压力/顶部共振用 `a-share-leverage-capitulation-analyst`，个人市场周期/过热纪律用 `user-investment-discipline`，最近 AI 消息核验用 `ai-chain-research-orchestrator`；公告、行情和本地数据技能只作相应证据伴侣。简单、常识性或非实时问题允许直接回答。
- 当用户明确提及同花顺 Financial API，或需要其支持的 A 股行情、财报、最新估值、指数/板块或基金数据时，优先将账户级 `hithink-finance` 作为数据伴侣；它是厂商数据，须保留来源与时点，决策关键数字仍须经 `financial-evidence-audit`，凭据只可从安全来源读取。
- 决策关键的市值、预期差、估值、覆盖率、单位换算或派生数字，必须附加 `financial-evidence-audit`；核验失败或口径冲突时不得准出确定性结论。
- 默认最多加载 1 个领域技能和 1 个数据、模型、文件或状态技能；命中决策关键数字时，`financial-evidence-audit` 作为额外强制准出门，不计作第二领域技能。不要使用固定技能栈，也不要为展示流程调用重复的通用推理技能。
- 非投研任务不要加载投研框架；按任务使用当前会话的全局技能、插件或原生工具。

## 投研硬边界

- 最新价格、估值、财报、订单、公告、政策、监管和市场状态必须联网或用当前本地数据核验，并标明时间。
- 官方公告、交易所、公司 IR、财报、招股书及官方客户/供应商材料是硬证据；行情厂商、概念标签、龙虎榜、社交和模型输出只能按其证据等级使用。
- 不把上游线索、概念关联或股价强度升级成公司客户、订单、收入或真实受益证据。
- 先确认上市主体、交易所和 ticker；排序时才分开基本面质量、业绩弹性和交易弹性。
- 缺少可靠数据时写 `N/A`、证据缺口和置信度，不补造精确数字。

## 工具与写入

- Grok/X、Gemini 只作实时线索、来源发现或反证；Codex 负责核验和结论。登录态网页默认使用用户已登录的 Chrome。
- TDX、iFinD、HTSC、东方财富、Yahoo、Alpha Vantage 和本地 HT 数据属于行情/厂商数据，除非它们直接返回官方原文。
- 账户、外部 watchlist、模拟交易、自动化、消息、云文件或其他外部写入必须有用户明确指令；不得无关读取或输出密钥、账户、密码、订单和日志。
- 本项目默认不创建 cron、heartbeat、邮件、推送或日报流水线；用户明确要求自动化时再进入相应工作流。

## 输出与维护

- 结论先行，按问题给证据、关键假设、最大风险和跟踪指标；快速问题不强行生成大表或完整报告。
- 所有自动化任务的最终对话结果、运行摘要、告警、失败原因和其他用户可见文本必须使用中文；ticker、代码、文件名、URL、字段名、精确标签和必要英文枚举可保留原文。
- 保持中文、路径和文档编码；实质修改后先报告并等待用户确认，再按账户级规则提交和推送。
