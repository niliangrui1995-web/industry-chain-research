# 产业链投研项目规则

## 技能使用

- 仓库技能位于 `.agents/skills/`，由 Codex 自动发现。
- 本项目覆盖账户级“每次先查四份路由文件”的细节：普通任务直接按当前技能列表和任务语义选择；仅在技能维护、技能缺失/冲突或路由确实无法判断时，才查询 `C:\Users\Administrator\.codex\skill-routing`。
- 投研任务可使用 `user-investment-framework`；简单、常识性或非实时问题允许直接回答。
- 默认最多加载 1 个领域技能和 1 个数据、文件或状态技能。不要使用固定技能栈，也不要为展示流程调用重复的通用推理技能。
- 非投研任务不要加载投研框架；按任务使用当前会话的全局技能、插件或原生工具。

## 投研硬边界

- 最新价格、估值、财报、订单、公告、政策、监管和市场状态必须联网或用当前本地数据核验，并标明时间。
- 官方公告、交易所、公司 IR、财报、招股书及官方客户/供应商材料是硬证据；行情厂商、概念标签、龙虎榜、社交和模型输出只能按其证据等级使用。
- 不把上游线索、概念关联或股价强度升级成公司客户、订单、收入或真实受益证据。
- 先确认上市主体、交易所和 ticker；排序时才分开基本面质量、业绩弹性和交易弹性。
- 缺少可靠数据时写 `N/A`、证据缺口和置信度，不补造精确数字。

## 工具与写入

- Grok/X、Gemini 只作实时线索、来源发现或反证；Codex 负责核验和结论。登录态网页默认使用用户已登录的 Chrome。
- TDX、iFinD、HTSC、东方财富、Yahoo、Alpha Vantage 和本地 HT 数据属于行情/厂商数据，除非它们直接返回官方原文。
- 账户、外部 watchlist、模拟交易、自动化、消息、云文件或其他外部写入必须有用户明确指令；不得无关读取或输出密钥、账户、密码、订单和日志。
- 本项目默认不创建 cron、heartbeat、邮件、推送或日报流水线；用户明确要求自动化时再进入相应工作流。

## 输出与维护

- 结论先行，按问题给证据、关键假设、最大风险和跟踪指标；快速问题不强行生成大表或完整报告。
- 保持中文、路径和文档编码；实质修改后先报告并等待用户确认，再按账户级规则提交和推送。
