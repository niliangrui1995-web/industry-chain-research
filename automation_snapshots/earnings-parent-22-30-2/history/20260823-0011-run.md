# 寡头财报单公司任务生成器运行报告

- Automation ID: `22-30-2`
- 运行结束（北京时间）: `2026-08-23T00:11:17+08:00`
- 最终状态: `ok/child_created`

## 运行版本预检

已在业务读取前读取 `docs/automation/AUTOMATION_RUN_CONTRACT.md` 并完成预检，结果如下（完整元数据）：

```json
{
  "captured_at_beijing": "2026-08-22T23:56:27+08:00",
  "prompt_contract_version": "2026-07-27.1",
  "skill_content_sha256": "1d8e93b9831ac4353aa9c196bb3cd2f9c82c03462bcc9100ebf927fe88d6b357",
  "skill_revision": "git:a5e0ac9ad86b7931c460b73ab404886aa711c148",
  "skill_tree_status": "clean",
  "skills": [
    "earnings-call-investment-analyst",
    "financial-evidence-audit"
  ],
  "status": "ok"
}
```

项目、紫金研选 venv、`requests`、`GlobalEarningsCalendarService` 与 `EarningsCalendarEvent` 的导入预检均通过。

## 72 小时筛选与官方时间核验

- 最终 guardrail dry-run 时间窗口：`2026-08-23 00:10` 至 `2026-08-26 00:10`（北京时间）。
- 按合同离线调用 `load_events(today=2026-08-22, lookahead_days=5, allow_network=False)`：返回 4 条事件，严格未来 72 小时候选为 1。
- 唯一候选：`SCHMID Group`（`SHMD`，`market=US`，`report_date=2026-08-25`，`fiscal_period=Jun/2026`，`time_label=盘前`）。`TASK_KEY=SHMD|2026-08-25|Jun/2026`；公司、ticker、market、寡头清单映射与去重均通过，`mapping_errors=[]`、`duplicate_task_keys={}`。
- 该事件是 `Nasdaq` 的 `estimated` 第三方日历线索。已检索公司 IR、公司投资者关系页面及 SEC EDGAR；截至本轮核验时未取得可保存的公司 IR / SEC / 交易所电话会或 webcast 精确时间，记为 `call_time_not_found` / `source_gap`，不把第三方时间升级为官方事实。
- 因无可用官方时间，未调用 `upsert_confirmed_event`，也未写入本地 confirmed event；按政策采用 `default_proxy_not_call_time`，计划北京时间为 `2026-08-25 23:30`。

## 子任务变更与双层回读

- 创建：`ec-shmd-20260825-a8ec3a`，名称为 `财报电话会深挖 SHMD SCHMID Group 2026-08-25`；更新 / 暂停：`0 / 0`。
- 子任务 prompt 由当前 `CHILD_PROMPT_TEMPLATE.md` 完整渲染：`template_body_exact=true`，含 `CHILD TASK SKILL HARD GATE`、`FINANCIAL EVIDENCE AUDIT HARD GATE`、下游需求、上游瓶颈、前季比较及审计产物/阻断字段。
- 配置门：`ACTIVE`、`model=gpt-5.6-terra`、`reasoning_effort=ultra`、`execution_environment=local`，一次性 RRULE 为 `RRULE:FREQ=WEEKLY;BYDAY=TU;BYHOUR=23;BYMINUTE=30;COUNT=1`，与计划北京时间匹配。
- 调度真值门：只读 `automations.next_run_at=1787671800000`，换算为北京时间 `2026-08-25 23:30`；调度状态同为 `ACTIVE`。最终 dry-run `status=ok`，无 `scheduler_status_mismatch`、`scheduler_next_run_mismatch`、坏 RRULE、重复键、模板或 source header 缺口。
- 正式 `automation_update` 创建请求超过两分钟未返回；中止后立即只读回查，确认没有产生 child，才使用项目允许的 `earnings_parent_guardrail.py --apply-mechanical` 受控 fallback。fallback 初次回读时调度器尚未登记，数秒后只读回查已登记并以上述最终 dry-run 闭环；未手工编辑 TOML 或 SQLite。

## 安全边界与失败项

- `refresh_events()`、`confirmed_events.json` 手工写入、SQLite 手工写入、`trade_dates` 与交易日历刷新：均未执行。
- 官方确认写回：`0`；失败 / 缺口：`SHMD` 尚无可用官方电话会或 webcast 精确时间，child 已按默认代理排期，并在 prompt header 标明 `Nasdaq`、`estimated` 与第三方日历 caveat。
