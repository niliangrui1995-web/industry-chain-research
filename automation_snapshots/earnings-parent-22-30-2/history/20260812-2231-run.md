# 22-30-2 财报母任务运行报告

- 运行时间（北京时间）：`2026-08-12T22:31:41+08:00` 至 `2026-08-12T22:50:46+08:00`
- 最终状态：`mechanical_guardrail_blocked`
- 母任务范围：仅进行离线日历读取、官方时间核验、单公司 child 生命周期维护和调度回读；不执行任何单公司财报分析。

## 运行版本合同与预检

已在业务读取和自动化写入前读取 `docs/automation/AUTOMATION_RUN_CONTRACT.md`，并执行：

```text
python scripts/automation_run_metadata.py --repo-root "D:\vcp_hunter\产业链投研" --skill earnings-call-investment-analyst --skill financial-evidence-audit --pretty
```

完整元数据：

```json
{
  "captured_at_beijing": "2026-08-12T22:31:41+08:00",
  "prompt_contract_version": "2026-07-27.1",
  "skill_content_sha256": "1d8e93b9831ac4353aa9c196bb3cd2f9c82c03462bcc9100ebf927fe88d6b357",
  "skill_revision": "git:c0e29d2a0eb9b73e06e4445d5065838f751224ba",
  "skill_tree_status": "clean",
  "skills": [
    "earnings-call-investment-analyst",
    "financial-evidence-audit"
  ],
  "status": "ok"
}
```

预检已确认 `D:\vcp_hunter\产业链投研`、`D:\vcp_hunter\紫金研选`、紫金研选 `.venv\Scripts\python.exe`、`requests`、`GlobalEarningsCalendarService`、`EarningsCalendarEvent` 和两个项目本地 Skill 可用。母任务只验证 child 的双 Skill 合同，未调用它们执行单公司分析。

## 离线日历与候选判断

主读取使用 `GlobalEarningsCalendarService().load_events(today=2026-08-11, lookahead_days=5, allow_network=False)`。读取时的严格窗口是 `2026-08-12 22:33` 至 `2026-08-15 22:33`（北京时间）；共读取 8 条事件，严格未来计划候选为 4 条源事件。guardrail dry-run 已通过映射、`TASK_KEY`、重复键、当前模板和 one-shot RRULE 的配置层检查；没有 ticker/company/market 映射错误。

| 源事件 | TASK_KEY | 计划北京时间 | 处理决定 |
|---|---|---|---|
| `COHR` Coherent | `COHR|2026-08-12|Jun/2026` | `2026-08-13 07:30` | 已有有效 child，待执行真值回读。 |
| `AMAT` Applied Materials | `AMAT|2026-08-13|Jul/2026` | `2026-08-14 07:30` | 官方时间已确认，待创建唯一 child。 |
| `3661.TW` Alchip（官方 Q2 说明会） | `3661.TW|2026-08-12|` | `2026-08-14 17:30` | 复用同一 TASK_KEY 的既有 child，完整模板更新并重排。 |
| `3661.TW` Alchip（Yahoo `estimated_conflict`） | `3661.TW|2026-08-14|` | `2026-08-14 20:00` | 不单独创建；官方 Q2 说明会已定位，避免为同一公司/季度创建重复 child。 |

## 官方时间核验与写回边界

- `COHR`：公司 IR 公告确认 2026-08-12 `4:30 p.m. ET`；本地已是 `confirmed`，离线读回为北京时间 `2026-08-13 04:30`，child 计划为 `07:30`。
- `AMAT`：公司 IR 活动页确认 2026-08-13 `4:30 PM EDT`；本地已是 `confirmed`，离线读回为北京时间 `2026-08-14 04:30`，child 计划为 `07:30`。
- `3661.TW`：公司 IR 页面确认 2026-08-14 `14:30 PM` 的线上 `2026 Q2 Institutional Investor Meeting`；本地已是 `confirmed`，离线读回为北京时间 `2026-08-14 14:30`，child 计划为 `17:30`。页面未另标 IANA 时区，按台湾主体页面语境使用 `Asia/Shanghai`（UTC+8）并保留该 caveat。

本轮未发现需要变更的已确认事件字段，因此没有调用 `upsert_confirmed_event()`；未手工改 `confirmed_events.json`。若需写回，只能由该服务接口完成并立即离线读回。

## 自动化变更与逐项双层回读

全部变更均使用 `codex_app__automation_update` 的完整 payload；新建或更新 prompt 均由本轮当前 `CHILD_PROMPT_TEMPLATE.md` 完整渲染，含事件 header、双 Skill hard gate、基础面/下游需求/上游瓶颈/前季比较/中文输出合同。每一项变更后均先读 `automation.toml`，再以只读 SQLite 查询调度器执行真值。

| 动作 | Child | TOML / 调度库回读 | 结果 |
|---|---|---|---|
| 创建 | `amat-applied-materials-2026-08-13` | `ACTIVE`；`RRULE:FREQ=WEEKLY;BYDAY=FR;BYHOUR=7;BYMINUTE=30;COUNT=1`；模型 `gpt-5.6-terra` / `ultra`；`next_run_at=2026-08-14 07:30` | 通过。 |
| 更新 | `3661-tw-alchip-2026-08-12` | `ACTIVE`；完整官方 header；`BYDAY=FR;BYHOUR=17;BYMINUTE=30;COUNT=1`；`next_run_at=2026-08-14 17:30` | 通过。旧 8 月 12 日预检查运行的 memory 明确要求会后重跑，故保持同一 child 的新计划。 |
| 验证 | `ec-cohr-20260812-f1f67b` | `ACTIVE`；`BYDAY=TH;BYHOUR=7;BYMINUTE=30;COUNT=1`；`next_run_at=2026-08-13 07:30` | 已有效，无需改动。 |
| 暂停 | `3044-tw-tripod-2026-08-11` | TOML=`PAUSED`，scheduler=`PAUSED`，`next_run_at=null` | 已完成 child，回读通过。 |
| 暂停 | `6871-t-mjc-2026-08-12` | TOML=`PAUSED`，scheduler=`PAUSED`，`next_run_at=null` | 已完成 child，回读通过。 |
| 暂停 | `crwv-coreweave-2026-08-11` | TOML=`PAUSED`，scheduler=`PAUSED`，`next_run_at=null` | 已完成 child，回读通过。 |
| 暂停 | `lite-lumentum-2026-08-11` | TOML=`PAUSED`，scheduler=`PAUSED`，`next_run_at=null` | 已完成 child，回读通过。 |

`3661.TW` 的 Yahoo `estimated_conflict`（`2026-08-14`）没有新建默认代理 child：公司 IR 已确认同一 Q2 results / investor meeting 的精确官方时间，且母任务一家公司只保留一个一次性 child，避免将第三方冲突估计扩展为重复分析任务。

## 未能收敛的历史残留

| Child | 支持接口结果 | 只读回查 | 处理 |
|---|---|---|---|
| `3017-tw-avc-2026-08-12` | 完整 `PAUSED` update 返回“automation does not exist in the app”。 | TOML 仍为 `ACTIVE`（正确 rrule `17:30`），scheduler 仍为 `ACTIVE`、旧 `next_run_at=2026-08-12 20:00`，且没有 `last_run_at`。 | `stale_child_paused` 未能执行；没有完成证据，未误报为完成，也没有重建重复 child。 |
| `1303-tw-nan-ya-2026-08-10` | 完整 `PAUSED` update 返回“automation does not exist in the app”。 | TOML=`PAUSED`，scheduler=`ACTIVE`，`next_run_at=null`。 | `child_scheduler_status_mismatch` 仍存在；未手工改 SQLite。 |

最终 guardrail dry-run 为 `mechanical_guardrail_blocked`：无映射错误、重复 TASK_KEY、坏 rrule、模板缺口或未来有效 child 的 `next_run_at` 不匹配；阻断项仅为上述 `3017.TW` / `1303.TW` 孤儿残留，以及 `3661.TW` 已有预检查 memory 所触发的保守 `active_with_memory` 标记。`3661.TW` 的当前 TOML 和 scheduler 已实际匹配新的官方会后计划。

## 写入边界

- 未调用 `refresh_events()`。
- 未触碰 `trade_dates`、`MarketCalendar.load_trade_dates()`、`MarketCalendar._schedule_trade_dates_refresh()` 或任何交易日历刷新路径。
- 调度数据库仅以 `mode=ro` 和 `PRAGMA query_only=ON` 回读；绝不手工写 SQLite。
