# 22-30-2 财报母任务运行报告

- 运行时间（北京时间）：2026-08-20T22:31:29+08:00 至 2026-08-20T22:40:42+08:00
- 最终状态：mechanical_guardrail_blocked
- 母任务范围：仅离线读取日历、核验官方时间、维护单公司 child 生命周期和回读调度真值；未执行单公司财报分析。

## 运行版本合同与预检

在任何业务读取或自动化写入前，已读取 `docs/automation/AUTOMATION_RUN_CONTRACT.md`，并执行：

    python scripts/automation_run_metadata.py --repo-root "D:\vcp_hunter\产业链投研" --skill earnings-call-investment-analyst --skill financial-evidence-audit --pretty

完整元数据：

    {
      "captured_at_beijing": "2026-08-20T22:31:29+08:00",
      "prompt_contract_version": "2026-07-27.1",
      "skill_content_sha256": "1d8e93b9831ac4353aa9c196bb3cd2f9c82c03462bcc9100ebf927fe88d6b357",
      "skill_revision": "git:558ddced76820e5be99ac7ebc330a0774339d47f",
      "skill_tree_status": "clean",
      "skills": [
        "earnings-call-investment-analyst",
        "financial-evidence-audit"
      ],
      "status": "ok"
    }

项目路径、紫金研选 `.venv\Scripts\python.exe`、`requests`、`GlobalEarningsCalendarService` 及两个项目本地 Skill 均可用。母任务只校验 child 的双 Skill 合同，未调用它们开展单公司分析。

## 离线日历、72 小时候选与官方时间

主读取直接调用 `GlobalEarningsCalendarService.load_events(today=2026-08-19, lookahead_days=5, allow_network=False)`；guardrail dry-run 以同一离线参数复核。严格未来窗口为北京时间 2026-08-20 22:40 至 2026-08-23 22:40；离线池共 0 条事件，未来候选为 0，映射错误、TASK_KEY 重复、待人工核验项和 soft probe 候选均为 0。

| 项目 | 结果 |
|---|---|
| 未来 72 小时候选 | 无 |
| 官方电话会依据 | 无候选，未发起新的公司 IR、SEC 或交易所查询。 |
| confirmed 写回 | 0；没有调用 `upsert_confirmed_event`，亦未手工修改 `confirmed_events.json`。 |
| 创建 / 更新 | 0 / 0；不存在可排期的 child。 |

## Child 生命周期、写回和调度回读

最终 dry-run 未发现坏 RRULE、静态合同、模板标记、source header、重复 TASK_KEY 或未来候选排期问题。所有既有 child 的模型/推理字段均符合 `gpt-5.6-terra` / `ultra`；RRULE 均为一次性 weekly `COUNT=1` 本地北京时间墙钟格式。

| 动作 | Child | 完成 / 调度证据 | 结果 |
|---|---|---|---|
| 应暂停 | `amat-applied-materials-2026-08-13` | DB `last_run_at=2026-08-14 07:30:06+08:00`；存在 `PENDING_REVIEW` 完成 inbox。TOML/DB 仍为 `ACTIVE`，`next_run_at=null`。 | 对正式 `automation_update mode=view` 的单项回读超过约 120 秒仍无返回后中止；未写 TOML 或 SQLite。 |
| 应暂停 | `3661-tw-alchip-2026-08-12` | DB `last_run_at=2026-08-14 17:30:21+08:00`；存在 `PENDING_REVIEW` 与后续 `ARCHIVED` 运行。TOML/DB 仍为 `ACTIVE`，`next_run_at=null`。 | 因正式接口回读不可用，未开始写入；待接口恢复后以同一 ID 完整更新并双回读。 |
| 不标记完成 | `3017-tw-avc-2026-08-12` | `last_run_at=null`、无 `automation_runs`；DB `next_run_at=2026-08-12 20:00:00+08:00`，与 header 的 17:30 历史排期不一致。 | 不猜测已完成，不暂停、不重建重复任务。 |
| 历史孤儿 | `1303-tw-nan-ya-2026-08-10` | TOML=`PAUSED`、scheduler=`ACTIVE`、`next_run_at=null`。 | `child_scheduler_status_mismatch`；未手改数据库。 |

本轮未成功创建、更新或暂停 child，因此不存在可准出的新写回或调度双回读。`mechanical_guardrail_blocked` 仅由上述既存/待暂停调度状态造成。

## 写入边界与后续

- 未调用 `refresh_events()`。
- 未触碰 `trade_dates`、`MarketCalendar.load_trade_dates()`、`MarketCalendar._schedule_trade_dates_refresh()` 或任何交易日历刷新路径。
- 未手工写 SQLite、`confirmed_events.json` 或 child TOML。
- 本轮仅新增本报告；已执行 child 的暂停留待 `automation_update` 接口恢复后处理。
